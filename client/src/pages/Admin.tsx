import React from 'react';
import {AdminTools} from "../modules";

export const Admin = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <AdminTools />
    </div>
  );
};
